# Renombrá este archivo a .env y completá los valores.
# Solo para usar desde tu PC (restaurar-fotos.bat / publicar-web.bat).

# Obligatorio para que las fotos no se pierdan en Render:
CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME

# Opcional: misma base de datos que Render (Postgres en Environment)
# DATABASE_URL=postgres://usuario:clave@host/nombre

# Opcional local:
# DJANGO_DEBUG=True
